#!/bin/bash

usage="./$(basename "$0") [-h] [-f FILE] [-d]
Script to perform bulk actions on Jira.
where:
    -h  show this help text
    -f  to use a specific file
    -d  use default template file if no file is specified or file is not found"
